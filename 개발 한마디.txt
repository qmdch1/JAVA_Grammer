Read only query부분은 네이티브 SQL쓰는게 가장 좋다. JPA 떡발하면 서버야 죽지마! 소리가 나옴
pk autoincrement보다 UUID를 써라
안느님은 JPA, jooq 조합으로 쓴다. Querydsl = jpql / jooq = native sql
csrf - secure httponly cookie에 refresh넣고 access는 그냥 변수에 넣으면 된다. 프론트 어딘가에 const token = fetch() 박으면 된다
DTO에서 Entity를 조회하다니....
1함수 1파라미터를 지키고 있기에 그런일은 몰루
